services:
  - type: web
    name: scraproute
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: scraproute-db
          property: connectionString
      - key: APP_PASSWORD
        sync: false

databases:
  - name: scraproute-db
    plan: free
